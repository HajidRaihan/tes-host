import React from 'react';
import { Spinner } from '@nextui-org/react';

const Loader = () => {
  return (
    <div>
      <Spinner size="lg" />
    </div>
  );
};

export default Loader;
